## 问题
- 主页顶部导航未显示，用户需要恢复原先的全局导航栏样式与位置。

## 解决方案
- 检查并恢复 `app/layout.tsx` 的全局 `<header>` 导航结构：品牌“天·才”、站点名与菜单（主页/学习记录/系统学习/知识库/统一对话）。
- 确保该导航在所有页面渲染（含主页），并保持 `sticky top-0`、`z-40`、浅色/暗色兼容。
- 移除首页内的重复导航（若存在），避免与全局导航冲突。
- 视觉不变：保留当前按钮与链接路径，字体与间距维持原样。

## 验证
- 打开首页与其他页面，导航始终可见且可滚动吸顶。
- 链接跳转正常，无重复导航或布局错位。

如确认，我将直接在 `app/layout.tsx` 恢复并固定全局导航栏。