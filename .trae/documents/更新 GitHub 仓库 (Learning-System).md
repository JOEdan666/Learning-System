我将为您把当前项目代码更新到名为 `Learning-System` 的 GitHub 仓库中。

检测到当前远程仓库地址为 `AI-System`，根据您的指示，我将把远程地址更改为 `Learning-System`。

### 执行计划

1.  **配置远程仓库**：
    *   将远程 `origin` 地址从 `git@github.com:JOEdan666/AI-System.git` 修改为 `git@github.com:JOEdan666/Learning-System.git`。
2.  **提交代码变更**：
    *   添加所有变动文件（包括新功能模块、UI 优化、数据库 Schema 更新以及清理的临时文件）。
    *   生成提交信息：`Update project: Add RAG question generation, optimize UI, and restructure codebase`。
3.  **推送到 GitHub**：
    *   将本地 `main` 分支推送到远程仓库。

### 注意事项
*   请确保您已经在 GitHub 上创建了名为 `Learning-System` 的空仓库（或者它已存在）。
*   我们将使用 SSH 方式推送（检测到您当前环境已配置 SSH）。
